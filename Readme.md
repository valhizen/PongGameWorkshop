#Instruction to Compile and run the code

```clone the repo```

In the root of the project
```
mkdir build
cd build
cmake ..
make
```

and run the executable you want

you need to change the shader load path in the project which you Shader.hpp
